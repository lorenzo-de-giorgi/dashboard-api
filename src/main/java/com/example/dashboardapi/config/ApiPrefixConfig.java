package com.example.dashboardapi.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder configuration.
 *
 * Nota: la logica di prefisso `/api/v1` è stata spostata in `ApiPrefixFilter`.
 * Questa classe esiste come placeholder per evitare problemi di compatibilità
 * con API di Spring diverse tra le versioni.
 */
@Configuration
public class ApiPrefixConfig {
    // Intentionally empty to avoid compile-time dependency on WebMvcRegistrations
}
